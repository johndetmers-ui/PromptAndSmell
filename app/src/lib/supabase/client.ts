// ---------------------------------------------------------------------------
// Browser-side Supabase Client (Demo Stub)
// ---------------------------------------------------------------------------
// In the full version this creates a real Supabase client. For the demo we
// export a no-op placeholder so that files that import it still compile.
// ---------------------------------------------------------------------------

export const supabase = null;
