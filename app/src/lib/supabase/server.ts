// ---------------------------------------------------------------------------
// Server-side Supabase Client (Demo Stub)
// ---------------------------------------------------------------------------
// In the full version this creates a real server-side Supabase client using
// cookies. For the demo we export a no-op placeholder.
// ---------------------------------------------------------------------------

export async function createServerClient() {
  return null;
}

export function createServiceClient() {
  return null;
}
