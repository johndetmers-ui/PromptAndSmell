# ---------------------------------------------------------------------------
# Prompt and Smell - Environment Variables
# ---------------------------------------------------------------------------
# Copy this file to .env.local and fill in the values for your environment.
# NEVER commit .env.local to version control.
# ---------------------------------------------------------------------------

# Supabase project URL (found in your Supabase dashboard under Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase anonymous (public) key (found in Settings > API > anon / public)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase service role key (found in Settings > API > service_role)
# WARNING: This key bypasses Row Level Security. Never expose it client-side.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Anthropic API key for Claude-powered scent generation
ANTHROPIC_API_KEY=your-anthropic-api-key-here
